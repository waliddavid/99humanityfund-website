---
title: "99% of Humanity Global Fund"
description: "A new institution is being built. Coming soon."
template: splash
---

<div class="coming-soon">

  <img src="/part-1.jpg" alt="99% of Humanity Global Fund" class="cs-hero" />

  <div class="cs-overlay">
    <p class="cs-eyebrow">99% of Humanity Global Fund</p>
    <h1 class="cs-title">Something is being built.</h1>
    <p class="cs-sub">A new institution — owned equally by everyone — is taking shape here. We are putting the final pieces in place.</p>
    <p class="cs-soon">Coming soon</p>
  </div>

</div>

<style>
{`
  /* ---- Full-screen takeover: hide all Starlight chrome on this page ---- */
  header.header,
  .sidebar-pane,
  .right-sidebar,
  .right-sidebar-container,
  nav.sidebar,
  .sl-flex.right-sidebar-panel,
  .pagination-links,
  footer { display: none !important; }

  /* Let the main content fill the viewport with no offsets */
  .main-frame,
  .main-pane,
  main { padding: 0 !important; margin: 0 !important; }

  html, body { margin: 0 !important; padding: 0 !important; }

  /* ---- The splash itself ---- */
  .coming-soon {
    position: fixed;
    inset: 0;
    width: 100vw;
    height: 100vh;
    overflow: hidden;
    background: var(--sl-color-black, #0f0e0a);
    z-index: 9999;
  }

  .cs-hero {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    filter: brightness(0.55);
  }

  .cs-overlay {
    position: absolute;
    inset: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: clamp(1.5rem, 5vw, 4rem);
    background: linear-gradient(
      180deg,
      rgba(15,14,10,0.45) 0%,
      rgba(15,14,10,0.30) 40%,
      rgba(15,14,10,0.55) 100%
    );
  }

  .cs-eyebrow {
    font-family: 'Source Serif 4', Georgia, serif;
    font-size: clamp(0.8rem, 1.6vw, 1rem);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.18em;
    color: #d9bd80;
    margin: 0 0 1.5rem;
    text-shadow: 0 2px 10px rgba(0,0,0,0.6);
  }

  .cs-title {
    font-family: 'Source Serif 4', Georgia, serif;
    font-weight: 700;
    font-size: clamp(2rem, 6vw, 4.5rem);
    line-height: 1.1;
    color: #fdfcf9;
    margin: 0 0 1.25rem;
    letter-spacing: -0.01em;
    text-shadow: 0 3px 16px rgba(0,0,0,0.6);
  }

  .cs-sub {
    font-family: 'Source Serif 4', Georgia, serif;
    font-size: clamp(1rem, 2.2vw, 1.4rem);
    font-style: italic;
    line-height: 1.5;
    color: #f3efe6;
    max-width: 34rem;
    margin: 0 0 2.5rem;
    text-shadow: 0 1px 10px rgba(0,0,0,0.65);
  }

  .cs-soon {
    font-family: 'Inter', -apple-system, sans-serif;
    font-size: clamp(0.75rem, 1.4vw, 0.95rem);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.28em;
    color: #d9bd80;
    border: 1px solid rgba(217, 189, 128, 0.5);
    border-radius: 999px;
    padding: 0.6rem 1.6rem;
    margin: 0;
    text-shadow: 0 1px 6px rgba(0,0,0,0.5);
  }
`}
</style>
